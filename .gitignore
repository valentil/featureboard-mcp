node_modules/
*.mcpb
.featureboard/
*.tmp-*

# NEVER commit the licensing private key
owner/keys/
owner/keys/private.pem
_syncprobe*
owner/issued-keys.json

# strengthen mode findings (FBMCPF-242)
strengthen_findings.json

# plugin build staging + built .plugin bundles (FBMCPF-244)
dist/
releases/

# auto-research outer loop (FBMCPF-246) — machine-local config + results
autoresearch.config.json
autoresearch_results.json
autoresearch_results.json.bak
autoresearch.log

# FBMCPF-254: sub-agent live-progress scratch channel (get_live_activity) — not project history
.fb-progress
